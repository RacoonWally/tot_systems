import workMessageList from "./mockDataSpam"
import spamMessageList from "./mockDataWork"

const getList = (name) => {
    switch (name) {
        case "work": {
            return
        }
        case "spam": {
            return
        }
        default: {
            return;
        }
    }

};
